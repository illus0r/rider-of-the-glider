import { IGIFFrame } from '@fand/gifuct-js';
export default class GIFPlayer {
    frames: IGIFFrame[];
    index: number;
    canvas: HTMLCanvasElement;
    ctx: CanvasRenderingContext2D;
    pixelRatio: number;
    startTime: number;
    playTime: number;
    static create(src: string, pixelRatio: number): Promise<GIFPlayer>;
    private constructor();
    getCanvas(): HTMLCanvasElement;
    update(): void;
}
